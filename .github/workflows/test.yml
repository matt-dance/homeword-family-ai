name: Tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  gateway:
    name: Gateway (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    defaults:
      run:
        working-directory: gateway
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install gateway
        run: pip install -e ".[dev]"
      - name: Run gateway tests
        run: ./scripts/test.sh

  web:
    name: Web (vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm run test:unit

  desktop:
    name: Desktop supervisor and packagers
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: desktop
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - name: Run supervisor tests
        run: go test ./...
      - name: Packager layout tests
        working-directory: .
        run: |
          bash desktop/scripts/bundle-macos_test.sh
          bash desktop/scripts/dmg-macos_test.sh
          bash desktop/scripts/bundle-linux_test.sh
          bash desktop/scripts/bundle-windows_test.sh

